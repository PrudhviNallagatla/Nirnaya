[project]
name = "nirnaya"
version = "0.0.0"
requires-python = ">=3.11"
description = "C++ interface contract verification tool"
readme = "README.md"

[tool.ruff]
line-length = 88

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
